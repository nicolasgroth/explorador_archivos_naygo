# Argumentos de CLI para naygo.exe — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `naygo.exe [<carpeta>] [--theme <id>] [--layout <nombre>] [--help] [--version]` — abrir una carpeta (override de sesión), aplicar tema/plantilla efímeros, y ayuda/versión; sin caerse por argumentos inválidos.

**Architecture:** Un parser puro en `core::cli` (`parse_args → CliArgs`, strings crudas, sin tocar catálogos) testeable al 100%. `main.rs` consume el parser, valida contra el catálogo de temas/plantillas, y aplica en orden (carpeta → layout → tema), juntando avisos de valores inválidos en un único diálogo. Tema/plantilla del argumento son efímeros (no tocan la config guardada). `--help`/`--version` muestran un diálogo y no abren la ventana.

**Tech Stack:** Rust workspace (naygo-core / naygo-platform / naygo-ui-slint), Slint 1.16 (render software). Build SIEMPRE con `CARGO_BUILD_JOBS=2`. Gate de cada tarea de código: `cargo fmt --all` + `cargo test -p naygo-core -p naygo-ui-slint -p naygo-platform` + `cargo clippy --workspace --all-targets -- -D warnings`.

**Spec:** `docs/superpowers/specs/2026-06-19-cli-args-design.md`

**Convenciones del repo (recordatorio):**
- Español neutral SIN voseo (código, comentarios, docs, UI). PowerShell 5.1: nunca `&&`/`||`.
- Header en archivos nuevos: `// Naygo — <descr>` + `// Copyright (c) 2026 Nicolás Groth / ISGroth. MIT License.`
- NO commitear `CLAUDE.md`. graphify-out/ y assets/icons/otros/ ya en .gitignore.
- NO `git push`. Commits locales por tarea. Mensajes terminan con `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Regla graphify: `graphify query "<pregunta>"` antes de grepear/leer fuentes; solo leer crudo tras orientarse o para modificar líneas concretas. Incluir esta regla en cada subagente.
- Tras tocar código: `graphify update .`.

**Datos reales ya explorados:**
- `crates/core/src/cli.rs` YA tiene: `first_positional(&[String]) -> Option<&str>`, `resolve_initial_dir(&[String], impl Fn(&Path)->bool) -> Option<PathBuf>`, `parse_initial_dir(&[String]) -> Option<PathBuf>`, con tests. Solo resuelve la carpeta.
- `crates/ui-slint/src/main.rs:165`: `let ctrl = Rc::new(RefCell::new(WorkspaceCtrl::new(start)));` — NO usa cli. Cerca de ahí está `logging::init()` y la lógica `if !load_session() { apply_first_run_layout() }` (de un bloque reciente).
- Temas: `naygo_core::theme::ThemeCatalog` (`available() -> &[ThemeId]`, `get`, `default_id`). El tema se PINTA con `crates/ui-slint/src/theme_apply.rs::apply(...)`. El cambio de tema desde el selector vive en `config_ctrl`/main.
- Plantillas: `TemplateStore`/`LayoutTemplate`; el menú de Layouts aplica una por nombre (`apply_template` o similar en workspace_ctrl).
- `rfd::MessageDialog` ya se usa (logging panic hook, confirmaciones).

---

## File Structure

| Archivo | Responsabilidad | Acción |
|---|---|---|
| `crates/core/src/cli.rs` | `CliArgs` + `parse_args` + `parse_args_real`; conserva lo existente. + tests. | Modificar |
| `crates/ui-slint/src/main.rs` | Consumir el parser; help/version (diálogo+return); aplicar dir/layout/theme; avisos. | Modificar |
| `crates/ui-slint/src/config_ctrl.rs` o `workspace_ctrl.rs` | Si los métodos de aplicar tema/plantilla persisten, una variante efímera. Solo si hace falta. | Modificar |
| `README.md`, `docs/GUIA-DE-USUARIO.md` | Documentar la sintaxis CLI. | Modificar |
| `CHANGELOG.md` | Entrada en "Sin publicar". | Modificar |

**Orden:** Task 1 (parser puro en core, testeable). Task 2 (cableado dir + help/version en main). Task 3 (theme + layout efímeros + avisos en main). Task 4 (docs + CHANGELOG).

---

## Task 1: `core::cli` — `CliArgs` + `parse_args`

**Files:**
- Modify: `crates/core/src/cli.rs`

- [ ] **Step 1: Escribir los tests primero (fallan: no existe parse_args)**

Agregar al `mod tests` de `cli.rs`:
```rust
    fn s(v: &str) -> String { v.to_string() }

    #[test]
    fn parse_sin_args_todo_vacio() {
        let a = parse_args(&[], |_| true);
        assert_eq!(a, CliArgs::default());
    }

    #[test]
    fn parse_solo_carpeta_valida() {
        let a = parse_args(&[s("D:\\dir")], |_| true);
        assert_eq!(a.dir, Some(PathBuf::from("D:\\dir")));
        assert_eq!(a.dir_arg_raw.as_deref(), Some("D:\\dir"));
        assert!(a.theme.is_none() && a.layout.is_none() && !a.help && !a.version);
    }

    #[test]
    fn parse_carpeta_invalida_dir_none_pero_raw_some() {
        let a = parse_args(&[s("D:\\no-existe")], |_| false);
        assert_eq!(a.dir, None);
        assert_eq!(a.dir_arg_raw.as_deref(), Some("D:\\no-existe"));
    }

    #[test]
    fn parse_theme_y_layout() {
        let a = parse_args(&[s("--theme"), s("winxp"), s("--layout"), s("Mi plantilla")], |_| true);
        assert_eq!(a.theme.as_deref(), Some("winxp"));
        assert_eq!(a.layout.as_deref(), Some("Mi plantilla"));
        assert_eq!(a.dir, None);
    }

    #[test]
    fn parse_orden_mezclado() {
        let a = parse_args(&[s("--theme"), s("x"), s("D:\\dir"), s("--layout"), s("y")], |_| true);
        assert_eq!(a.dir, Some(PathBuf::from("D:\\dir")));
        assert_eq!(a.theme.as_deref(), Some("x"));
        assert_eq!(a.layout.as_deref(), Some("y"));
    }

    #[test]
    fn parse_flag_sin_valor_se_ignora_sin_panic() {
        let a = parse_args(&[s("--theme")], |_| true);
        assert_eq!(a.theme, None);
        let b = parse_args(&[s("--layout")], |_| true);
        assert_eq!(b.layout, None);
    }

    #[test]
    fn parse_help_y_version() {
        assert!(parse_args(&[s("--help")], |_| true).help);
        assert!(parse_args(&[s("--version")], |_| true).version);
    }

    #[test]
    fn parse_flag_desconocido_se_ignora() {
        let a = parse_args(&[s("--zzz"), s("D:\\dir")], |_| true);
        assert_eq!(a.dir, Some(PathBuf::from("D:\\dir")));
    }

    #[test]
    fn parse_valor_de_theme_no_es_carpeta() {
        // El token tras --theme es su valor, NO la carpeta posicional.
        let a = parse_args(&[s("--theme"), s("D:\\dir")], |_| true);
        assert_eq!(a.theme.as_deref(), Some("D:\\dir"));
        assert_eq!(a.dir, None);
        assert_eq!(a.dir_arg_raw, None);
    }
```

- [ ] **Step 2: Correr — fallan**
```
$env:CARGO_BUILD_JOBS = "2"; cargo test -p naygo-core cli
```
Esperado: FAIL (no existe `CliArgs`/`parse_args`).

- [ ] **Step 3: Implementar `CliArgs` + `parse_args` + `parse_args_real`**

Agregar a `cli.rs` (deja `first_positional`/`resolve_initial_dir`/`parse_initial_dir` como están):
```rust
/// Argumentos de línea de comandos ya parseados. `theme`/`layout` son las cadenas CRUDAS del
/// argumento (sin validar contra el catálogo: eso lo hace la UI, que sí lo conoce).
#[derive(Debug, Default, Clone, PartialEq, Eq)]
pub struct CliArgs {
    /// Carpeta posicional, SOLO si es un directorio existente (según `is_dir`).
    pub dir: Option<PathBuf>,
    /// El token posicional tal cual, aunque no sea un dir válido (para avisar "no es carpeta").
    pub dir_arg_raw: Option<String>,
    pub theme: Option<String>,
    pub layout: Option<String>,
    pub help: bool,
    pub version: bool,
}

/// Parsea los argumentos (SIN el ejecutable). `is_dir` valida la carpeta posicional (inyectable
/// para test puro). Reglas: `--help`/`--version` ponen su flag; `--theme <v>`/`--layout <v>`
/// consumen el siguiente token como valor (si no hay, se ignoran); el primer token que no sea
/// flag ni valor-de-flag es la carpeta posicional. Flags desconocidos se ignoran. Nunca panic.
pub fn parse_args(args: &[String], is_dir: impl Fn(&Path) -> bool) -> CliArgs {
    let mut out = CliArgs::default();
    let mut i = 0;
    while i < args.len() {
        let arg = args[i].as_str();
        match arg {
            "--help" | "-h" => out.help = true,
            "--version" | "-v" => out.version = true,
            "--theme" => {
                if let Some(v) = args.get(i + 1) {
                    out.theme = Some(v.clone());
                    i += 1;
                }
            }
            "--layout" => {
                if let Some(v) = args.get(i + 1) {
                    out.layout = Some(v.clone());
                    i += 1;
                }
            }
            // Flag desconocido: ignorar (no consume valor).
            _ if arg.starts_with("--") => {}
            // Primer posicional no vacío = carpeta candidata (solo el primero).
            _ if !arg.is_empty() && out.dir_arg_raw.is_none() => {
                out.dir_arg_raw = Some(arg.to_string());
                let path = PathBuf::from(arg);
                if is_dir(&path) {
                    out.dir = Some(path);
                }
            }
            _ => {}
        }
        i += 1;
    }
    out
}

/// Atajo de producción: usa `Path::is_dir`.
pub fn parse_args_real(args: &[String]) -> CliArgs {
    parse_args(args, |p| p.is_dir())
}
```

- [ ] **Step 4: Tests pasan**
```
$env:CARGO_BUILD_JOBS = "2"; cargo test -p naygo-core cli
```
Esperado: todos PASS (los nuevos + los viejos de `cli`).

- [ ] **Step 5: Gate + commit**
```
$env:CARGO_BUILD_JOBS = "2"; cargo fmt -p naygo-core
$env:CARGO_BUILD_JOBS = "2"; cargo clippy -p naygo-core --all-targets -- -D warnings
```
```bash
git add crates/core/src/cli.rs
git commit -F - <<'EOF'
feat(core): parse_args para los argumentos de linea de comandos

CliArgs{dir, dir_arg_raw, theme, layout, help, version} + parse_args(args, is_dir)
+ parse_args_real. Carpeta posicional, flags --theme/--layout (consumen valor),
--help/--version; flags desconocidos y flags sin valor se ignoran sin panic. Puro
y testeado.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
```

---

## Task 2: main.rs — consumir args, carpeta override, --help/--version

**Files:**
- Modify: `crates/ui-slint/src/main.rs`

- [ ] **Step 1: Explorar (graphify) los puntos de main**

Corre:
- `graphify query "main startup default start dir WorkspaceCtrl new load_session apply_first_run_layout"`
- `graphify query "navigate active pane to a directory method"`
Identifica los nombres REALES: la variable `start` (carpeta por defecto, ~main.rs:165), cómo se construye el ctrl, dónde está `load_session()`/`apply_first_run_layout()`, y el método para navegar el panel activo a una carpeta (p. ej. `navigate_active_to(PathBuf)`).

- [ ] **Step 2: Parsear los args al inicio de main + help/version**

Cerca del inicio de `main` (tras `logging::init()` y la captura del offset de zona si está ahí), añade:
```rust
    // Argumentos de línea de comandos.
    let cli_args = naygo_core::cli::parse_args_real(&std::env::args().skip(1).collect::<Vec<_>>());
    if cli_args.help {
        rfd::MessageDialog::new()
            .set_level(rfd::MessageLevel::Info)
            .set_title("Naygo — opciones")
            .set_description(cli_help_text())
            .set_buttons(rfd::MessageButtons::Ok)
            .show();
        return Ok(());
    }
    if cli_args.version {
        rfd::MessageDialog::new()
            .set_level(rfd::MessageLevel::Info)
            .set_title("Naygo")
            .set_description(format!(
                "Naygo v{}\nNicolás Groth / ISGroth · MIT",
                env!("CARGO_PKG_VERSION")
            ))
            .set_buttons(rfd::MessageButtons::Ok)
            .show();
        return Ok(());
    }
```
> Ajusta el tipo de retorno: si `main` devuelve `Result<(), ...>`, usa `return Ok(())`; si no, `return;`. Mira la firma real de `main`. Coloca esto ANTES de crear la ventana (no debe abrirse en help/version).

Agrega el helper del texto de ayuda (en main.rs):
```rust
/// Texto de ayuda de la línea de comandos (para el diálogo de --help).
fn cli_help_text() -> String {
    "Uso: naygo.exe [<carpeta>] [opciones]\n\n\
     <carpeta>        Abre esa carpeta al iniciar (debe existir).\n\
     --theme <id>     Aplica un tema solo en esta sesión (no cambia tu configuración).\n\
     --layout <nombre>  Aplica una plantilla de disposición de paneles.\n\
     --help           Muestra esta ayuda.\n\
     --version        Muestra la versión.\n\n\
     Los valores que no existan se ignoran y la app abre igual."
        .to_string()
}
```
(Si quieres, lista los temas disponibles dinámicamente con `ThemeCatalog::available()`; opcional, no bloqueante.)

- [ ] **Step 3: La carpeta del argumento manda sobre la sesión**

Donde hoy se decide la carpeta de arranque / sesión (`start` + `load_session()`/`apply_first_run_layout()`), aplica:
```rust
    // Si se pasó una carpeta válida por argumento, manda sobre la sesión: tras construir el
    // workspace, navega el panel activo a esa carpeta.
    if let Some(dir) = cli_args.dir.clone() {
        ctrl.borrow_mut().navigate_active_to(dir);   // usa el método REAL de navegación
    }
```
> Colócalo DESPUÉS de construir el ctrl y de la rama de sesión/clásico (así sobre-escribe lo que esa rama dejó en el panel activo). Usa el nombre real del método (`navigate_active_to` u otro). Si navegar requiere que el listado arranque, `navigate_active_to` ya lo hace (mira cómo lo usa el resto).

- [ ] **Step 4: Build + gate**
```
$env:CARGO_BUILD_JOBS = "2"; cargo build -p naygo-ui-slint
$env:CARGO_BUILD_JOBS = "2"; cargo fmt --all
$env:CARGO_BUILD_JOBS = "2"; cargo test -p naygo-core -p naygo-ui-slint -p naygo-platform
$env:CARGO_BUILD_JOBS = "2"; cargo clippy --workspace --all-targets -- -D warnings
```
Esperado: compila, tests PASS, clippy limpio.

- [ ] **Step 5: Commit**
```bash
git add crates/ui-slint/src/main.rs
git commit -F - <<'EOF'
feat(cli): main consume los argumentos — carpeta override + --help/--version

naygo.exe <carpeta> abre esa carpeta en el panel activo (manda sobre la sesion).
--help y --version muestran un dialogo y no abren la ventana.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
```

---

## Task 3: main.rs — --theme y --layout efímeros + avisos

**Files:**
- Modify: `crates/ui-slint/src/main.rs` (y `config_ctrl.rs`/`workspace_ctrl.rs` si hace falta una variante efímera)

- [ ] **Step 1: Explorar (graphify) cómo aplicar tema y plantilla**

Corre:
- `graphify query "apply theme by id set active theme without persisting config selector"`
- `graphify query "apply layout template by name from layouts menu template store builtin and user"`
Identifica: (1) el método que aplica un `ThemeId` al runtime (el que usa el selector de config) y SI persiste (llama `save()`); (2) el método que aplica una plantilla por nombre y SI persiste la plantilla por defecto; (3) cómo se listan las plantillas built-in + usuario para buscar por nombre. Reporta los nombres reales.

- [ ] **Step 2: Aplicar --layout (si existe la plantilla)**

Tras la rama de la carpeta (Task 2), antes o después según corresponda (el layout arma la disposición; conviene ANTES de navegar la carpeta para que el dir caiga en el panel activo de la nueva disposición — reordena si es necesario):
```rust
    let mut avisos: Vec<String> = Vec::new();
    if let Some(name) = cli_args.layout.as_deref() {
        if ctrl.borrow_mut().apply_layout_by_name(name) {   // método REAL; devuelve true si existía
            // ok
        } else {
            avisos.push(format!("La plantilla \"{name}\" no existe; se ignoró."));
        }
    }
```
> `apply_layout_by_name` es el nombre PLACEHOLDER. Busca el método real que aplica una plantilla por nombre (el del menú de Layouts). Si ese método NO devuelve si existió, envuélvelo: busca la plantilla en el store (built-in + usuario) y aplica solo si la encuentras; si no, agrega el aviso. Si aplicar la plantilla persiste la "plantilla por defecto", usa la variante que NO persista (o aplica la disposición sin guardar). Reporta qué método usaste y si tuviste que crear una variante efímera.
> ORDEN dir+layout: si hay layout, aplícalo primero (arma paneles), y la navegación de la carpeta (Task 2) va después sobre el panel activo de esa disposición. Ajusta el orden de los bloques en main para que sea: construir ctrl → sesión/clásico → layout (si hay) → navegar carpeta (si hay) → tema (si hay).

- [ ] **Step 3: Aplicar --theme efímero (si existe el id)**

```rust
    if let Some(id) = cli_args.theme.as_deref() {
        if ctrl.borrow().theme_id_exists(id) {        // método REAL: ThemeCatalog::available contiene id
            ctrl.borrow_mut().apply_theme_ephemeral(id);  // aplica SIN persistir
        } else {
            avisos.push(format!("El tema \"{id}\" no existe; se usó el predeterminado."));
        }
    }
```
> `theme_id_exists`/`apply_theme_ephemeral` son PLACEHOLDERS. Busca: cómo el selector aplica un tema (probablemente `config.set_theme(id)` + `theme_apply::apply(...)` + `save()`). Para la variante efímera: aplica el tema al runtime (set en memoria + `theme_apply::apply` para repintar) PERO sin llamar `save()`. Si el método actual siempre persiste, crea una variante (`apply_theme_ephemeral`) que haga lo mismo sin guardar. Para validar el id, `ThemeCatalog::available()` contiene los ThemeId disponibles. Reporta los nombres reales y si creaste la variante.
> IMPORTANTE: tras aplicar tema/layout, asegúrate de que la UI se repinte (si hay un `sync_rows`/`apply_theme` que se llama al cambiar tema en el selector, replícalo aquí).

- [ ] **Step 4: Avisos en un diálogo + log**

Tras aplicar todo, y antes de `ui.run()`:
```rust
    // Aviso de carpeta inválida (token posicional que no era un dir).
    if cli_args.dir.is_none() {
        if let Some(raw) = cli_args.dir_arg_raw.as_deref() {
            avisos.push(format!("La ruta \"{raw}\" no es una carpeta; se ignoró."));
        }
    }
    for a in &avisos {
        crate::logging::log_line(&format!("CLI: {a}"));
    }
    if !avisos.is_empty() {
        rfd::MessageDialog::new()
            .set_level(rfd::MessageLevel::Warning)
            .set_title("Naygo — argumentos")
            .set_description(avisos.join("\n"))
            .set_buttons(rfd::MessageButtons::Ok)
            .show();
    }
```
> Coloca el bloque de avisos donde tenga sentido en el flujo (tras aplicar dir/layout/theme). El diálogo es síncrono; aparece una vez antes de mostrar la ventana, y la app sigue.

- [ ] **Step 5: Build + gate completo**
```
$env:CARGO_BUILD_JOBS = "2"; cargo build -p naygo-ui-slint
$env:CARGO_BUILD_JOBS = "2"; cargo fmt --all
$env:CARGO_BUILD_JOBS = "2"; cargo test -p naygo-core -p naygo-ui-slint -p naygo-platform
$env:CARGO_BUILD_JOBS = "2"; cargo clippy --workspace --all-targets -- -D warnings
```
Esperado: compila, tests PASS, clippy limpio.

- [ ] **Step 6: Verificación manual (recomendada)**

Compila release y prueba:
```
$env:CARGO_BUILD_JOBS = "2"; cargo build --release -p naygo-ui-slint
```
Lanza: `target\release\naygo.exe "D:\Empresas"` (abre esa carpeta); `... --theme winxp` (tema XP esta sesión); `... --theme nopolexiste` (aviso + abre); `... --layout <nombre real>`; `... --help`; `... --version`. Reporta lo que viste.

- [ ] **Step 7: Commit (main.rs + config_ctrl/workspace_ctrl si tocaste)**
```bash
git add crates/ui-slint/src/main.rs
git commit -F - <<'EOF'
feat(cli): --theme y --layout efimeros + avisos de argumentos invalidos

--theme aplica un tema solo en la sesion (sin persistir); --layout aplica una
plantilla por nombre (built-in o de usuario). Valores inexistentes y carpeta
invalida se juntan en un aviso (dialogo + log) y la app abre igual.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
```

---

## Task 4: Documentación + CHANGELOG

**Files:**
- Modify: `README.md`, `docs/GUIA-DE-USUARIO.md`, `CHANGELOG.md`

- [ ] **Step 1: README — sección de uso por línea de comandos**

En `README.md`, en la sección de instalación/uso, agrega un bloque:
```markdown
## Uso por línea de comandos

`naygo.exe` acepta argumentos opcionales:

​```
naygo.exe [<carpeta>] [--theme <id>] [--layout <nombre>] [--help] [--version]
​```

- `<carpeta>`: abre Naygo en esa carpeta (debe existir).
- `--theme <id>`: aplica un tema solo en esa sesión (no cambia tu configuración). Ids: los
  temas disponibles en Configuración → Apariencia.
- `--layout <nombre>`: aplica una plantilla de disposición de paneles (de las del menú Layouts).
- `--help`, `--version`: muestran un diálogo con la ayuda o la versión.

Los valores que no existan se ignoran (la app abre igual y deja un aviso).
```
(Quita el `​` invisible de las vallas si lo copias; usa vallas ``` reales.)

- [ ] **Step 2: GUÍA — una nota breve**

En `docs/GUIA-DE-USUARIO.md`, agrega una línea en la sección apropiada (cerca de instalación o al final): "Naygo se puede lanzar desde un acceso directo o terminal con una carpeta (`naygo.exe "D:\\carpeta"`) y opciones `--theme`/`--layout`; ver el README para el detalle."

- [ ] **Step 3: CHANGELOG — "Sin publicar"**

En `CHANGELOG.md`, bajo `## [Sin publicar]` → `### Añadido`:
```markdown
- Argumentos de línea de comandos: `naygo.exe <carpeta>` abre esa carpeta al iniciar, y
  `--theme`/`--layout` aplican un tema o una disposición solo para esa sesión. `--help` y
  `--version` muestran la información correspondiente. Útil para accesos directos y para
  "Abrir en Naygo".
```

- [ ] **Step 4: Verificar voseo + commit**
```
rg -ni "\b(arrastrá|escribí|elegí|podés|tenés|querés|hacé|volvé)\b" README.md docs/GUIA-DE-USUARIO.md CHANGELOG.md
```
Esperado: sin resultados.
```bash
git add README.md docs/GUIA-DE-USUARIO.md CHANGELOG.md
git commit -F - <<'EOF'
docs: documentar los argumentos de linea de comandos

README con la sintaxis y opciones; nota en la guia; entrada de CHANGELOG.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
```

---

## Verificación final del bloque

- [ ] Gate completo verde:
```
$env:CARGO_BUILD_JOBS = "2"; cargo fmt --all; cargo test -p naygo-core -p naygo-ui-slint -p naygo-platform; cargo clippy --workspace --all-targets -- -D warnings
```
- [ ] Sin voseo en lo nuevo (rg en los archivos tocados).
- [ ] `graphify update .`.
- [ ] Verificación manual de Nicolás: lanzar con carpeta, --theme, --theme inválido, --layout, --help, --version.

## Notas de cierre

- Actualizar la memoria del backlog (`feature-backlog-cli-args`): marcar implementado.
- Con esto el backlog queda vacío. Sugerir a Nicolás regenerar el dist si quiere probar el CLI
  en la VM (sobre todo el "Abrir en Naygo" del menú contextual, que ahora usa el argumento).
```
