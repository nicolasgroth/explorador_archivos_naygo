// Naygo — panel Inspector (Fase 2b): propiedades del ítem enfocado del panel activo.
// Copyright (c) 2026 Nicolás Groth / ISGroth. MIT License.
import { InspectorVm } from "types.slint";

// Una fila etiqueta/valor del inspector.
component PropRow inherits HorizontalLayout {
    in property <string> label;
    in property <string> value;
    spacing: 8px;
    padding: 2px;
    Text {
        text: root.label;
        color: #9ab;
        font-weight: 700;
        width: 84px;
        horizontal-alignment: right;
        vertical-alignment: center;
    }
    Text {
        text: root.value;
        color: #dde;
        vertical-alignment: center;
        horizontal-stretch: 1;
        overflow: elide;
    }
}

export component InspectorPanel inherits Rectangle {
    in property <bool> active;
    in property <InspectorVm> info;
    background: #0e1622;
    border-width: root.active ? 1.5px : 0px;
    border-color: #4f8ae0;

    VerticalLayout {
        padding: 8px;
        spacing: 4px;
        Text {
            text: "Propiedades";
            color: #9ab;
            font-weight: 700;
            height: 22px;
            vertical-alignment: center;
        }
        if !root.info.present: Text {
            text: "Sin selección";
            color: #678;
            vertical-alignment: center;
        }
        if root.info.present: VerticalLayout {
            spacing: 2px;
            PropRow { label: "Nombre"; value: root.info.name; }
            PropRow { label: "Tipo"; value: root.info.kind; }
            PropRow { label: "Tamaño"; value: root.info.size; }
            PropRow { label: "Modificado"; value: root.info.modified; }
            PropRow { label: "Creado"; value: root.info.created; }
            PropRow { label: "Ruta"; value: root.info.path; }
        }
        // Empuja el contenido hacia arriba.
        Rectangle { vertical-stretch: 1; }
    }
}
