// Naygo — tipos compartidos UI<->Rust (Slint). Copyright (c) 2026 N. Groth / ISGroth. MIT.
export struct RowData {
    name: string,
    ext: string,
    size: string,
    modified: string,
    is-dir: bool,
    selected: bool,
    focused: bool,
}
